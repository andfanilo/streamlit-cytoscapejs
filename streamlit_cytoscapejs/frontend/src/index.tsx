import React from "react"
import ReactDOM from "react-dom"
import StreamlitCytoscape from "./StreamlitCytoscape"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitCytoscape />
  </React.StrictMode>,
  document.getElementById("root")
)
